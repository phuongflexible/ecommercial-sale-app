@using EcommSale.Models;
@model Brand
@{
    ViewData["Title"] = "Delete";
}
<br />
<h2 class="text-black">Delete Brand</h2>

@if (!string.IsNullOrEmpty(ViewBag.DeleteError))
{
    <div class="alert alert-danger">@ViewBag.DeleteError</div>
}

<form asp-action="Delete" method="post">
    <div class="p-4 rounded border">
        <input type="hidden" asp-for="BrandID" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group row mb-3">
            <div class="col-2">
                <label asp-for="BrandName"></label>
            </div>
            <div class="col-5">
                <input asp-for="BrandName" readonly="readonly" class="form-control" />
            </div>
            <span asp-validation-for="BrandName" class="text-danger"></span>
        </div>
        <div class="form-group mb-3">
            <input type="submit" class="btn btn-danger" value="Delete" onclick="return confirm('Are you sure you want to delete the brand @(Model.BrandName)?');" />
            <a asp-action="Index" class="btn btn-green">Back to list</a>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
